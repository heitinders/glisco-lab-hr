# Components Context
- ui/ = shadcn-style primitives (headless Radix + Tailwind)
- layout/ = app shell components (sidebar, topbar, breadcrumbs)
- Module components in their own directories
- All components use CVA for variant styling
- Use lucide-react for icons
